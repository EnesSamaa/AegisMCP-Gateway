apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aegis-gateway.fullname" . }}-config
  labels:
    {{- include "aegis-gateway.labels" . | nindent 4 }}
data:
  aegis.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
